"use strict";
let input = prompt("Введите '0':");
// input == 0 срабатывает для строки "0". Для строгой проверки числа:
alert(input === "0" ? "Это строка '0'" : Number(input) === 0 ? "Это число 0" : "Не ноль");